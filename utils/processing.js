import Constants from "expo-constants";
/*
{
    "model": "text-davinci-003",
    "prompt": "Write a funny and goofy two to three sentences description of a person who is 26 years old, asian, female, and currently happy.",
    "temperature": 0.75,
    "max_tokens": 60,
    "top_p": 1,
    "frequency_penalty": 0.5,
    "presence_penalty": 0.5
}
*/

// import { Audio } from 'expo-av';

// async function processAudioFile(filePath) {
//   const soundObject = new Audio.Sound();

//   try {
//     await soundObject.loadAsync({
//       uri: filePath,
//     });

//     const duration = soundObject.getDurationMillis();
//     const interval = 100; // interval in milliseconds

//     // Start the sound and begin processing at intervals
//     await soundObject.playAsync();
//     setInterval(async () => {
//       const decibelLevel = await soundObject.getDecibelLevelAsync();
//       console.log(`Decibel level: ${decibelLevel}`);
//     }, interval);
//   } catch (error) {
//     console.error(error);
//   }
// }
