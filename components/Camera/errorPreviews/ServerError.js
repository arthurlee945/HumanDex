import React from "react";
import { View, Text } from "react-native";

function ServerError({ defaultFont }) {
    return (
        <View>
            <Text style={defaultFont}>ServerError</Text>
        </View>
    );
}

export default ServerError;
